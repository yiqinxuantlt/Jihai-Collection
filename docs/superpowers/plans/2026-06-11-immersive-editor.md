# Immersive Editor Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade the editor page into a calmer immersive writing space with focus mode, creation metrics, and refined rich-text ergonomics.

**Architecture:** Keep the static vanilla JavaScript app. Add one editor view state flag, extend the editor topbar/sidebar renderers, and update CSS for normal and focus-mode layouts.

**Tech Stack:** HTML generated by vanilla JavaScript, CSS, `contenteditable`, localStorage, Node `node:test`.

---

## Files

- Modify: `assets/app.js`
  - Add `editorFocusMode`, focus mode toggle, creation status cards, and side word count syncing.
- Modify: `assets/styles.css`
  - Polish editor paper, topbar, side panel, focus mode, and floating toolbar.
- Verify: `tests/app.test.cjs`
  - Existing tests should remain green.

## Tasks

- [ ] Add `editorFocusMode` to normalized state.
- [ ] Add a focus mode button to the editor topbar.
- [ ] Hide secondary topbar actions in focus mode.
- [ ] Convert metadata panel into a creation sidebar with metrics and metadata fields.
- [ ] Sync side word count and update timestamp from `updateEditorDerived`.
- [ ] Add focus-mode CSS that centers and widens the paper while hiding the sidebar.
- [ ] Refine rich editor spacing, prose blocks, and floating toolbar styling.
- [ ] Run `node --test .\tests\app.test.cjs`.
- [ ] Reload the in-app browser and inspect editor layout.

## Self-Review

- Covers the approved “沉浸写作舱” direction.
- Does not alter bookshelf or book-detail navigation.
- No placeholder implementation steps remain.
